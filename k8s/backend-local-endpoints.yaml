apiVersion: v1
kind: Endpoints
metadata:
  name: backend-local
  namespace: default
subsets:
  - addresses:
      - ip: 10.250.0.10   # <-- your LAPTOP_IP
    ports:
      - name: http
        port: 8080        # <-- your BACKEND_PORT
        protocol: TCP
